# Fixture: a spec whose status line lags the DEC that adopted it (DEC-035)

**Status:** proposal, drafted 2026-09-19; **not yet adopted** — DEC-033 is the
decision this spec awaits.

This is the stale-state class: the value (a DEC number) is right, but the
adopted state is wrong. The status line names DEC-033, whose log heading records
that the spec was adopted, so the pre-adoption phrasing is a contradiction.
