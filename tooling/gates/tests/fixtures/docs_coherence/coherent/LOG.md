## DEC-014 - Target model is pythia-70m-deduped; the 160M plan was unbuildable

The probed model is **`pythia-70m-deduped`** (`d_model=512`, `n_layers=6`).
Every "Pythia-160M" reference in the handoff doc is superseded. There is no
pythia-160m release and no non-deduped pythia-70m release.
